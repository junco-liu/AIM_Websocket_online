# PRODUCT.md — AIM 控制台

## 产品

浏览器直连 VEX AIM 机器人的局域网控制台。用户在自己的设备上打开网页，输入机器人 IP，
直接通过四个 WebSocket 控制机器人并观看其摄像头画面。**没有后端**：网页只是静态资源，
所有连接由用户浏览器发起，数据不经过任何第三方。

与官方 Python 库的差异：官方方案要求装 Python 环境、跑脚本；本控制台开箱即用，
打开网页就能控制，这是它存在的理由。

## 平台

`web`。桌面与移动**双一等支持**，移动端体验不得降级。

## 使用者与场景

**对外公开，任何人都可能打开。** 这是设计的支配性事实：

- 大量用户是首次接触，不知道要先让机器人和自己在同一局域网，不知道 IP 从哪来
- 首次使用引导、空状态、错误恢复说明必须是认真设计的一等内容，不是补丁
- 「未连接」是高频状态，不是短暂过渡态，要当成一个真正的页面来设计
- 容错优先于密度：宁可控件少、目标大，也不要误触导致机器人乱跑

**环境光：明亮教室 / 白天室内。** 强环境光下的反光屏幕。主题应从这个现场推导。

## 任务

主任务是**边看画面边驾驶**。用户的眼睛在摄像头画面上，手在方向控制上。
其余功能（灯光、踢球、速度调节）是低频的伴随操作。

## 能力（全部必须保留，一个不能少）

| 能力 | 说明 |
|---|---|
| 连接 | 输入 IP，连接 / 断开；显示连接状态 |
| 遥测 | 电量、朝向；轮询式，200ms |
| 画面 | 开启 / 关闭摄像头推流 |
| 移动 | 前后左右平移、左右转、急停；按住即走、松开即停 |
| 键盘 | WASD 平移、QE 转向、空格急停 |
| 速度 | 移动速度 mm/s（20–200）、转向速度 °/s（20–180） |
| 灯光 | 6 颗灯珠，单独或整体设置颜色 / 熄灭 |
| 踢球 | 轻 / 中 / 重三档 |
| 日志 | 活动与错误记录 |

## 状态

`未连接` · `连接中` · `已连接未推流` · `已连接推流中` · `连接失败` · `连接中断`

六个状态都要有明确的视觉表达和恢复路径，不能只靠一行文字区分。

## 约束

- **技术栈**：优先单文件、零依赖、零构建（当前 737 行）。若重设计后超过约 1000 行，
  允许改用 Vue + 构建。栈的选择由行数结果决定，不预先承诺。
- **托管**：Chrome 147+ 的 Local Network Access 规则决定了页面来源必须是安全上下文
  （https）或与机器人同地址空间。详见 README。
- **无鉴权**：机器人本身没有任何鉴权，页面对能访问到它的人开放控制权。
- **术语**：沿用 VEX 官方叫法 —— 灯珠、踢球、朝向。

## 用户已声明的视觉约束

「简约美观」。除此之外无绑定的品牌资产、字体或配色要求。

## 成功标准

一个从没见过这个页面的人，在没有说明文档的情况下，能自己完成
「连接 → 看到画面 → 把机器人开动」这条路径。
